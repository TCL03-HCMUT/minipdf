from .main import app     # or import and call whatever function drives the CLI

if __name__ == "__main__":  # this check is technically redundant here,
    app()                   # but it makes the file usable as a script too